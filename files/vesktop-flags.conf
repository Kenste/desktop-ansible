# Disable WebRTC automatic gain control and audio processing
--disable-features=WebRtcAGC
--agc-startup-min-volume=0

# Wayland and PipeWire screen capture support
--enable-features=UseOzonePlatform,WebRTCPipeWireCapturer,WaylandWindowDecorations
--ozone-platform-hint=auto
